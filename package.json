{
  "name": "fetch",
  "version": "1.0.0",
  "description": "A project to fetch a page from GitHub Pages.",
  "main": "express-server.js",
  "scripts": {
    "start": "node express-server.js",
    "build": "rm -rf build && mkdir build && rsync -av --progress --exclude build --exclude node_modules ./ ./build"
  },
  "dependencies": {
    "express": "^4.17.1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/petehuu/fetch.git"
  },
  "author": "petehuu",
  "license": "MIT"
}
